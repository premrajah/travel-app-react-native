import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
        marginVertical: 4
    },
    icon: {
        width: 20,
        height: 20
    },
    title: {
        marginHorizontal: 10
    }
})

export default styles