import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        marginLeft: 32
    },
    subtitle: {
        fontSize: 20,
        color: "#000000",
        marginTop: 30,
    },
    row: {
        flexDirection: "row",
        flexWrap: "wrap"
    }
});

export default styles;